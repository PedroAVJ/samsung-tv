# Third-party notices

The runtime depends on [ws](https://github.com/websockets/ws), pinned to 8.21.3
in `package.json` and `package-lock.json`. It is distributed by its own authors
under the MIT license. The dependency is installed from its package source;
its source and copyright notice are preserved in the installed package. It is
not first-party artwork or proprietary Samsung code.
